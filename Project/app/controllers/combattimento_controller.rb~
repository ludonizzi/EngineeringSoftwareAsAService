class CombattimentoController < ApplicationController
    before_action :authenticate_user!

    def combatti
        id = params[:id]
        @user= current_user
        @user.vittorie = @user.id
        @user.sconfitte = 5
    end

end
